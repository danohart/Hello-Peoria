# import * from './generated/prisma.graphql'

type Mutation {
    createPlace(name: String, description: String, address: String, image: String, category: String): Place!
    # updatePlace(id: ID!, name: String, description: String, address: String, category: String): Place!
    deletePlace(id: ID!): Place
}

type Query {
    places: [Place]!
    place(where: PlaceWhereUniqueInput!): Place
}