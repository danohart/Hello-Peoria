# import * from './generated/prisma.graphql'

type Mutation {
    createPlace(name: String, description: String, address: String, image: String): Place!
}

type Query {
    places: [Place]!
    place(where: PlaceWhereUniqueInput!): Place
}